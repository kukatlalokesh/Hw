# Week 2 NumPy and Pandas Summary

## NumPy
- Created 1D and 2D arrays with `np.array()`.
- Inspected `shape`, `size`, and `ndim`.
- Used indexing and slicing.
- Performed array addition, subtraction, multiplication, and division.
- Used `reshape()` and `flatten()`.
- Generated random integers and floats with NumPy.
- Calculated mean, median, and standard deviation.
- Performed matrix addition and multiplication.

## Pandas Series
- Created Series objects.
- Accessed values by index.
- Filtered Series using conditions.
- Inspected Series values and indexes.

## Pandas DataFrames
- Created DataFrames from dictionaries and lists.
- Inspected rows and columns.
- Read CSV data.
- Used `head()`, `tail()`, `info()`, and `describe()`.
- Selected single and multiple columns.
- Filtered rows with conditions.
- Identified, filled, and dropped missing values.
- Renamed columns.
- Sorted records by one or more columns.
- Created calculated columns.
- Used `groupby()` for counts and averages.

## Week 2 Takeaways
NumPy provides efficient array-based numerical operations, while pandas provides convenient tools for tabular data cleaning, analysis, and transformation.
