# Save Charts to File

Run the script with:

python save_charts.py

The program creates a charts folder and saves the generated chart as monthly_sales.png.

The PNG file is created when the Python script is run, so generated image files do not need to be committed to Git.
